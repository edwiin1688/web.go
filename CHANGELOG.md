# Changelog

此專案的所有顯著變更將記錄在此檔案中。

格式基於 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.0.0/)。

## [未發布]

### Added
- 新增 `.gitignore` 忽略專案編譯出的同名執行檔 `/web.go`（針對非 Windows 系統編譯產生的二進位檔）。
- 新增 `.gitignore` 忽略 `.python-version` 與 `go.work.sum`。
- 新增 `.gitignore` 忽略常見 IDE 與編輯器個人設定（如 `/.vscode/`）。
- 新增 `.gitignore` 忽略常見作業系統快取與暫存檔（如 `.DS_Store`、`Thumbs.db`）。
- 新增 `.gitignore` 忽略 Python 虛擬環境目錄（如 `.venv/`、`venv/`、`env/`）。
